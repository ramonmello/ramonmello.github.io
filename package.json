{
  "name": "ramonmello-dev-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter \"./packages/*\" --filter \"./apps/*\" dev",
    "build": "pnpm --parallel --filter \"./packages/*\" --filter \"./apps/*\" build"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
